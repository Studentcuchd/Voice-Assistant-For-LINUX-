# Voice-Controlled Linux Assistant - Python dependencies
#
# Install all Python dependencies at once:
#   pip install -r requirements.txt
#
# Linux system packages still required (not installable via pip):
#   Debian/Ubuntu:
#     sudo apt update
#     sudo apt install -y portaudio19-dev python3-pyaudio xdotool
#
#   Fedora:
#     sudo dnf install -y portaudio-devel python3-pyaudio xdotool
#
# Notes:
# - SQLite is built into Python; no pip package needed.
# - xdotool is required for desktop automation plugin actions.

SpeechRecognition>=3.10.0,<4.0.0
PyAudio>=0.2.13,<1.0.0
PyAutoGUI>=0.9.54,<1.0.0
